version: '3.8'

services:

  movie-app:
    build: .
    container_name: movie-app
    ports:
      - "8080:8080"

    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db.glqgahhxxrvbipywixly.supabase.co:5432/postgres?sslmode=require
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: Ajay9291@$1

    restart: always